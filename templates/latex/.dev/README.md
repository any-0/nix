This dev environment was created from my (Julian) nix config: `g.any-0.com/nix`.

Preferences:

- Build outputs (.log, .toc etc.) go into `.build/`.
- Images go into `images/`.
- Miscellaneous non-build files go into `misc/`.
